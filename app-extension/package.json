{
  "name": "replace-with-extension-name",
  "packageManager": "pnpm@9.15.4",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "pnpm --config.dlx-cache-max-age=0 dlx @stackable-labs/cli-app-extension@latest dev"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
